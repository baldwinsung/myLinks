# myLinks
leverage python to create a html page and display a table of web site links using a comma separated file

## csv file format
website,description,order-group-number

if you want to group big tech together. end the row with the same number. 
then group another set of companies with the same number.

check example.csv for more information

## syntax example
./main.py -c example.csv -o example.html
